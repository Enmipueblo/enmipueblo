---
import Layout from "../layouts/Layout.astro";
import DestacadosHomeIsland from "../components/DestacadosHomeIsland.tsx";
import GoogleAdIsland from "../components/GoogleAdIsland.tsx";
---

<Layout title="EnMiPueblo - Encuentra servicios en tu pueblo">
  <main class="bg-gradient-to-b from-emerald-50 via-white to-white">
    <!-- HERO -->
    <section class="py-16 md:py-20">
      <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row items-center gap-10">
        <div class="flex-1 space-y-4">
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-emerald-900 leading-tight">
            Servicios y profesionales<br class="hidden md:block" />
            <span class="text-emerald-600">cerca de tu pueblo</span>
          </h1>
          <p class="text-gray-600 text-base md:text-lg max-w-xl">
            EnMiPueblo conecta a vecinos con pequeños negocios, autónomos y
            profesionales locales. Busca, contacta y apoya a quienes trabajan
            en tu zona.
          </p>

          <div class="flex flex-col sm:flex-row gap-3 mt-4">
            <a
              href="/buscar"
              class="inline-flex items-center justify-center px-6 py-3 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-semibold text-sm md:text-base shadow"
            >
              Buscar servicios
            </a>
            <a
              href="/ofrecer"
              class="inline-flex items-center justify-center px-6 py-3 rounded-xl bg-white border border-emerald-200 text-emerald-800 hover:bg-emerald-50 font-semibold text-sm md:text-base"
            >
              Ofrecer un servicio
            </a>
          </div>

          <div class="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-3 text-sm text-gray-600">
            <div class="flex items-start gap-2">
              <span class="mt-1 text-emerald-600">✔</span>
              <span>Anuncios moderados desde el panel de administración.</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="mt-1 text-emerald-600">✔</span>
              <span>Visibilidad para negocios y autónomos locales.</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="mt-1 text-emerald-600">✔</span>
              <span>Contacto directo por email o WhatsApp.</span>
            </div>
          </div>
        </div>

        <!-- Lado derecho: tarjeta ejemplo -->
        <div class="flex-1 w-full max-w-md mx-auto">
          <div class="bg-white border border-emerald-100 rounded-3xl shadow-lg p-5 space-y-3">
            <p class="text-xs font-semibold text-emerald-700 uppercase tracking-wide">
              Ejemplo de anuncio
            </p>
            <h2 class="text-lg font-bold text-emerald-900">
              Reformas y arreglos generales
            </h2>
            <p class="text-sm text-gray-600">
              Pintura, pequeñas reformas, mantenimiento del hogar y
              arreglos urgentes en tu pueblo o alrededores.
            </p>
            <div class="flex flex-wrap gap-2 text-xs text-gray-500">
              <span class="px-2 py-1 bg-emerald-50 text-emerald-700 rounded-full border border-emerald-100">
                Pueblo cercano
              </span>
              <span class="px-2 py-1 bg-emerald-50 text-emerald-700 rounded-full border border-emerald-100">
                Reformas
              </span>
            </div>
            <div class="mt-2 text-xs text-gray-400">
              Así se verán los servicios que publiquen tus vecinos.
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- SERVICIOS DESTACADOS EN PORTADA -->
    <section class="py-12 border-t border-emerald-100 bg-white">
      <div class="max-w-6xl mx-auto px-4">
        <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-3">
          <div>
            <h2 class="text-2xl md:text-3xl font-bold text-emerald-900">
              Servicios destacados en portada
            </h2>
            <p class="mt-1 text-sm text-gray-600 max-w-xl">
              Solo aparecen aquí los anuncios que tú marques como
              <strong> destacados en portada </strong>
              desde el panel de administración. El resto de servicios se
              encuentran en la página de búsqueda.
            </p>
          </div>

          <a
            href="/buscar"
            class="inline-flex items-center justify-center px-4 py-2 rounded-xl bg-emerald-50 text-emerald-800 border border-emerald-200 hover:bg-emerald-100 text-sm font-semibold mt-2 md:mt-0"
          >
            Ver todos los servicios
          </a>
        </div>

        <DestacadosHomeIsland client:load />

        <!-- 1 anuncio máximo en Home (zona “segura”: después del contenido principal) -->
        <div class="mt-8">
          <GoogleAdIsland client:load />
        </div>
      </div>
    </section>
  </main>
</Layout>
