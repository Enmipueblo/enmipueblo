---
import Layout from "../layouts/Layout.astro";
import DestacadosHomeIsland from "../components/DestacadosHomeIsland.tsx";
import GoogleAdIsland from "../components/GoogleAdIsland.tsx";

const SITE = "https://enmipueblo.com";

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": `${SITE}#website`,
      url: SITE,
      name: "EnMiPueblo",
      inLanguage: "es-ES",
      potentialAction: {
        "@type": "SearchAction",
        target: `${SITE}/buscar?texto={search_term_string}`,
        "query-input": "required name=search_term_string",
      },
    },
    {
      "@type": "Organization",
      "@id": `${SITE}#org`,
      name: "EnMiPueblo",
      url: SITE,
      logo: `${SITE}/favicon.svg`,
    },
  ],
};
---

<Layout
  title="EnMiPueblo - Encuentra servicios en tu pueblo"
  description="Encuentra y ofrece servicios locales en EnMiPueblo. Busca profesionales cerca de tu pueblo y contacta por email o WhatsApp."
>
  <script type="application/ld+json" is:inline>
    {JSON.stringify(jsonLd)}
  </script>

  <main class="relative">
    <section class="py-16 md:py-20">
      <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row items-center gap-10">
        <div class="flex-1 space-y-4">
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-extrabold text-slate-900 leading-tight">
            Servicios y profesionales<br class="hidden md:block" />
            <span class="text-emerald-800">cerca de tu pueblo</span>
            <span class="text-amber-400">.</span>
          </h1>

          <p class="text-slate-700 text-base md:text-lg max-w-xl">
            EnMiPueblo conecta a vecinos con pequeños negocios, autónomos y
            profesionales locales. Busca, contacta y apoya a quienes trabajan
            en tu zona.
          </p>

          <div class="flex flex-col sm:flex-row gap-3 mt-4">
            <a
              href="/buscar"
              class="inline-flex items-center justify-center px-6 py-3 rounded-2xl bg-emerald-200 hover:bg-emerald-300 border border-emerald-200 text-slate-900 font-extrabold text-sm md:text-base shadow-sm"
            >
              Buscar servicios
            </a>
            <a
              href="/ofrecer"
              class="inline-flex items-center justify-center px-6 py-3 rounded-2xl bg-white/45 backdrop-blur border border-emerald-200 text-slate-900 hover:bg-white/60 font-extrabold text-sm md:text-base shadow-sm"
            >
              Ofrecer un servicio
            </a>
          </div>

          <div class="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-3 text-sm text-slate-700">
            <div class="flex items-start gap-2">
              <span class="mt-1 text-emerald-700">✔</span>
              <span>Anuncios moderados desde el panel de administración.</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="mt-1 text-emerald-700">✔</span>
              <span>Visibilidad para negocios y autónomos locales.</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="mt-1 text-emerald-700">✔</span>
              <span>Contacto directo por email o WhatsApp.</span>
            </div>
          </div>
        </div>

        <div class="flex-1 w-full max-w-md mx-auto">
          <div class="bg-white/45 backdrop-blur border border-emerald-200 rounded-3xl shadow-[0_16px_40px_-28px_rgba(15,23,42,0.35)] p-5 space-y-3">
            <p class="text-xs font-extrabold text-emerald-800 uppercase tracking-wide">
              Ejemplo de anuncio
            </p>
            <h2 class="text-lg font-extrabold text-slate-900">
              Reformas y arreglos generales
            </h2>
            <p class="text-sm text-slate-700">
              Pintura, pequeñas reformas, mantenimiento del hogar y
              arreglos urgentes en tu pueblo o alrededores.
            </p>

            <div class="flex flex-wrap gap-2 text-xs">
              <span class="px-2 py-1 bg-emerald-100/70 text-slate-900 rounded-full border border-emerald-200">
                Pueblo cercano
              </span>
              <span class="px-2 py-1 bg-amber-100/80 text-amber-950 rounded-full border border-amber-200">
                Reformas
              </span>
            </div>

            <div class="mt-2 text-xs text-slate-600">
              Así se verán los servicios que publiquen tus vecinos.
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="py-12 border-t border-emerald-200 bg-white/25 backdrop-blur">
      <div class="max-w-6xl mx-auto px-4">
        <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-3">
          <div>
            <h2 class="text-2xl md:text-3xl font-extrabold text-slate-900">
              Servicios destacados en portada
            </h2>
            <p class="mt-1 text-sm text-slate-700 max-w-xl">
              Una selección de anuncios destacados para ayudarte a encontrar <strong>servicios de confianza</strong> en los pueblos de toda España.
            </p>
          </div>

          <a
            href="/buscar"
            class="inline-flex items-center justify-center px-4 py-2 rounded-2xl bg-emerald-100/75 border border-emerald-200 text-slate-900 hover:bg-emerald-100 text-sm font-extrabold mt-2 md:mt-0 shadow-sm"
          >
            Ver todos los servicios
          </a>
        </div>

        <DestacadosHomeIsland client:load />

        <div class="mt-8">
          <GoogleAdIsland client:load />
        </div>
      </div>
    </section>
  </main>
</Layout>
