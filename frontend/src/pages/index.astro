---
import Layout from "../layouts/Layout.astro";
import DestacadosHomeIsland from "../components/DestacadosHomeIsland.tsx";
import GoogleAdIsland from "../components/GoogleAdIsland.tsx";

const SITE = "https://enmipueblo.com";

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": `${SITE}#website`,
      url: SITE,
      name: "EnMiPueblo",
      inLanguage: "es-ES",
      potentialAction: {
        "@type": "SearchAction",
        target: `${SITE}/buscar?texto={search_term_string}`,
        "query-input": "required name=search_term_string",
      },
    },
    {
      "@type": "Organization",
      "@id": `${SITE}#org`,
      name: "EnMiPueblo",
      url: SITE,
      logo: `${SITE}/favicon.svg`,
    },
  ],
};
---

<Layout
  title="EnMiPueblo - Encuentra servicios en tu pueblo"
  description="Encuentra y ofrece servicios locales en EnMiPueblo. Busca profesionales cerca de tu pueblo y contacta por email o WhatsApp."
>
  <script type="application/ld+json" is:inline>
    {JSON.stringify(jsonLd)}
  </script>

  <main class="relative">
    <section class="py-16 md:py-20">
      <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row items-center gap-10">
        <div class="flex-1 space-y-4">
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-extrabold leading-tight" style="color: var(--sb-ink);">
            Encuentra tu Servicio Ideal<span style="color: var(--sb-accent);">.</span>
          </h1>

          <p class="text-base md:text-lg max-w-xl" style="color: var(--sb-ink2);">
            Filtra por texto, categoría y ubicación. Rápido y simple.
          </p>

          <div class="flex flex-col sm:flex-row gap-3 mt-4">
            <a
              href="/buscar"
              class="inline-flex items-center justify-center px-6 py-3 rounded-2xl font-extrabold text-sm md:text-base shadow-sm border hover:opacity-95 transition"
              style="background: var(--sb-blue); color:#EFFFFB; border-color: rgba(30,64,175,0.25);"
            >
              Buscar servicios
            </a>
            <a
              href="/ofrecer"
              class="inline-flex items-center justify-center px-6 py-3 rounded-2xl font-extrabold text-sm md:text-base shadow-sm border hover:bg-white transition"
              style="background: var(--sb-card); color: var(--sb-ink); border-color: var(--sb-border);"
            >
              Ofrecer un servicio
            </a>
          </div>

          <div class="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-3 text-sm" style="color: var(--sb-ink2);">
            <div class="flex items-start gap-2">
              <span class="mt-1" style="color: var(--sb-green);">✔</span>
              <span>Anuncios moderados desde el panel de administración.</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="mt-1" style="color: var(--sb-accent);">✔</span>
              <span>Visibilidad para negocios y autónomos locales.</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="mt-1" style="color: var(--sb-blue);">✔</span>
              <span>Contacto directo por email o WhatsApp.</span>
            </div>
          </div>
        </div>

        <div class="flex-1 w-full max-w-md mx-auto">
          <div
            class="rounded-3xl p-6 border shadow-[0_16px_40px_-28px_rgba(0,0,0,0.35)] space-y-3"
            style="background: var(--sb-card2); border-color: var(--sb-border);"
          >
            <p class="text-xs font-extrabold uppercase tracking-wide" style="color: var(--sb-ink2);">
              Ejemplo de anuncio
            </p>

            <h2 class="text-lg font-extrabold" style="color: var(--sb-ink);">
              Reformas y arreglos generales
            </h2>

            <p class="text-sm" style="color: var(--sb-ink2);">
              Pintura, pequeñas reformas, mantenimiento del hogar y arreglos urgentes en tu pueblo o alrededores.
            </p>

            <div class="flex flex-wrap gap-2 text-xs">
              <span class="px-3 py-1 rounded-full border font-semibold"
                style="background: rgba(185,247,215,0.70); color: var(--sb-ink); border-color: rgba(10,127,79,0.20);">
                Pueblo cercano
              </span>
              <span class="px-3 py-1 rounded-full border font-semibold"
                style="background: rgba(90,208,230,0.22); color: var(--sb-ink); border-color: rgba(19,167,168,0.25);">
                Reformas
              </span>
              <span class="px-3 py-1 rounded-full border font-semibold"
                style="background: rgba(207,239,255,0.65); color: var(--sb-ink); border-color: rgba(30,64,175,0.18);">
                Local
              </span>
            </div>

            <div class="mt-2 text-xs" style="color: rgba(17,75,95,0.92);">
              Así se verán los servicios que publiquen tus vecinos.
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="py-12">
      <div class="max-w-6xl mx-auto px-4">
        <div
          class="rounded-3xl p-6 md:p-7 border shadow-[0_18px_50px_-38px_rgba(0,0,0,0.45)]"
          style="background: rgba(255,255,255,0.55); border-color: var(--sb-border); backdrop-filter: blur(10px);"
        >
          <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-3">
            <div>
              <h2 class="text-2xl md:text-3xl font-extrabold" style="color: var(--sb-ink);">
                Servicios destacados en portada
              </h2>
              <p class="mt-1 text-sm max-w-xl" style="color: var(--sb-ink2);">
                Una selección de anuncios destacados para ayudarte a encontrar <strong>servicios de confianza</strong> en los pueblos de toda España.
              </p>
            </div>

            <a
              href="/buscar"
              class="inline-flex items-center justify-center px-4 py-2 rounded-2xl text-sm font-extrabold shadow-sm border hover:bg-white transition"
              style="background: rgba(255,255,255,0.72); color: var(--sb-ink); border-color: var(--sb-border);"
            >
              Ver todos los servicios
            </a>
          </div>

          <DestacadosHomeIsland client:load />

          <div class="mt-8">
            <GoogleAdIsland client:load />
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>
