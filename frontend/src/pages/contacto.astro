---
// src/pages/contacto.astro
import Layout from "../layouts/Layout.astro";
import ContactFormIsland from "../components/ContactFormIsland.tsx";
import GoogleAdIsland from "../components/GoogleAdIsland.tsx";
---

<Layout title="Contacto - EnMiPueblo">
  <section class="relative py-16 min-h-screen flex flex-col items-center justify-center space-y-10 overflow-hidden">
    <!-- Fondo nueva identidad -->
    <div
      class="absolute inset-0 pointer-events-none"
      style="
        background:
          radial-gradient(950px circle at 18% 12%, rgba(185,247,215,0.26) 0%, rgba(0,0,0,0) 62%),
          radial-gradient(900px circle at 82% 18%, rgba(90,208,230,0.20) 0%, rgba(0,0,0,0) 60%),
          radial-gradient(1100px circle at 20% 95%, rgba(30,64,175,0.08) 0%, rgba(0,0,0,0) 72%),
          linear-gradient(to bottom, rgba(255,255,255,0.30), rgba(255,255,255,0.06));
      "
    ></div>

    <div class="relative w-full flex flex-col items-center justify-center space-y-10 px-4">
      <GoogleAdIsland client:idle />
      <ContactFormIsland client:idle />

      <noscript>
        <div class="w-full max-w-3xl mx-auto">
          <div
            class="p-4 rounded-2xl border text-center"
            style="background: rgba(255,255,255,0.78); border-color: var(--sb-border); color: var(--sb-ink2);"
          >
            Para enviar el formulario necesitas habilitar JavaScript.
            También puedes escribirnos a
            <a class="underline" href="mailto:serviciosenmipueblo@gmail.com" style="color: var(--sb-blue);">
              serviciosenmipueblo@gmail.com
            </a>.
          </div>
        </div>
      </noscript>
    </div>
  </section>
</Layout>
