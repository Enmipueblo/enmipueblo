server {
  listen 8080;
  server_name _;

  # Evita Location: http://host:8080/... en redirects (ej: /contacto -> /contacto/)
  absolute_redirect off;
  port_in_redirect off;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
